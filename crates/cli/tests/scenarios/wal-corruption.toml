name = "wal-corruption-recovery"

# Simulate partial writes and recovery
[[responses]]
pattern = { type = "contains", text = "checkpoint" }
response = "Checkpoint saved"

[[responses]]
pattern = { type = "any" }
response = "Recovered from corruption"
